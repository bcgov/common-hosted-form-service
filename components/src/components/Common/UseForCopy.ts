const UseForCopy = {
    weight: 10,
    type: 'checkbox',
    defaultValue: true,
    label: 'Allow value propagation',
    tooltip: 'When a submission is duplicated',
    key: 'validate.isUseForCopy',
    input: true
};

export default UseForCopy;
